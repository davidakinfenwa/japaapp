
import 'package:flutter/material.dart';

class CodedTalkProvider extends ChangeNotifier {
  // static CodedTalkListData? _codedTalkListData;

  // CodedTalkListData get codedTalkListData =>
  //     _codedTalkListData ?? CodedTalkListData.empty();
  // set codedTalkListData(CodedTalkListData talks) {
  //   _codedTalkListData = talks;
  //   notifyListeners();
  // }
}
